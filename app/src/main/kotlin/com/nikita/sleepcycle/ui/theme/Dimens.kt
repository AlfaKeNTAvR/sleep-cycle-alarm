package com.nikita.sleepcycle.ui.theme

import androidx.compose.ui.unit.dp

// File purpose: named spacing/size constants for the UI layer, so no composable hardcodes a raw dp value.

val ScreenHorizontalPadding = 20.dp
val ScreenBottomPadding = 28.dp
val ScreenContentGap = 16.dp

val CardCornerRadius = 20.dp
val CardPadding = 20.dp
val CardNarrowPadding = 16.dp
val CardContentGap = 10.dp
val CardBorderWidth = 1.dp

val PrimaryButtonHeight = 64.dp
val PrimaryButtonCornerRadius = 32.dp
val SecondaryButtonHeight = 56.dp
val SecondaryButtonCornerRadius = 28.dp

val ChipHeight = 48.dp
val ChipCornerRadius = 12.dp
val ChipGap = 8.dp

val MinTouchTarget = 48.dp

/** Hatch stroke spacing on disabled sleep-length chips: about 1 mm apart on a typical phone density. */
val HatchLineSpacing = 6.dp

/** Hatch stroke width on disabled sleep-length chips: about as thin as the chip label's text strokes. */
val HatchLineWidth = 1.dp

val TimelineBarHeight = 8.dp
val TimelineBarCornerRadius = 4.dp
val StatusDotSize = 8.dp

val DividerThickness = 1.dp
